* text=auto eol=lf
# Windows scripts keep CRLF (cmd.exe) and their UTF-8 BOM (Windows PowerShell 5.1 reads Korean text only with it)
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.png binary
*.jpg binary
*.mp4 binary
*.npz binary
