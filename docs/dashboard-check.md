# 대시보드 확인 (runs/fixture-perp, 2026-09-22)

서버: `PORT=8765 python dashboard/server.py runs/fixture-perp` (WSL, 커밋 9586dbe). 브라우저: Claude 데스크톱 내장 브라우저, 뷰포트 1600×900 → 800×900 순서로 확인.

`/state.json`: `venue=orangex-perp`, `fly_name=초파리`, `orders_today=4`, `disclosure` 5줄, `events` 30개(스킵 행 0).

- [x] 콘솔 오류 0 (1600×900, 800×900 모두 — `createImageData IndexSizeError` 없음)
- [x] 한자 없음(문서 전체 `[一-鿿㐀-䶿]` 0개), `<html lang="ko">`, computed font-family `"Pretendard Variable", Pretendard, system-ui, …`
- [x] 상단 스트립: D+1, 30번째 관측, 순자산 492.70 USDT (-50.7%), 오늘 매매 4, 다음 관측 표시
- [x] 포지션 카드: 롱 0.042 BTC, 진입가 76,162.6, 마크가 76,185.8, 청산가 72,718.06, 미실현 +0.97 USDT (0.6%), 누적 수수료 −11.98, 누적 펀딩 +0.00, 강제청산 2회, 건너뛴 관측 0, 다음 펀딩 카운트다운
- [x] 결정 배너 `관망 HOLD` (마지막 틱). 픽스처 30틱 안에서 롱 진입/뒤집기/강제청산 배너는 클립·마커로 확인(✕ 2개 = 틱 13·21 강제청산, ▲ 3개, ▼ 1개)
- [x] 순자산 차트: 폴리라인 3개(매수호가·순자산·BTC 단순 보유 벤치마크), ✕ 마커 2개. ◇ 펀딩·● 방 이벤트는 픽스처에 없어 미검증 → 리허설 런(`runs/rehearsal`)에서 확인
- [x] 하단 공시 5줄 표시(`#disclosure` span 5개), 이벤트 0개여도 렌더되는 순서(`if (!e) return;` 앞)
- [x] 뷰포트 800×900: 뇌 캔버스 723×542로 리사이즈 후 정상 렌더(픽셀 샘플 31% 발광), 카드가 단일 컬럼으로 재배치, 오류 없음
- [x] Network: 외부 요청 0, 폰트 `assets/fonts/PretendardVariable.woff2` 519,960 B(서브셋)

남은 항목(리허설·시즌에서 확인): 파산 오버레이·BANKRUPT 클립, ◇ 펀딩 마커(00:00 UTC 경계), ● 방 이벤트(순자산 1,200 돌파).

## 2026-09-22 수정 웨이브 후 재확인(1920×1080 무대 모드)

시즌 캐시 락(`perp_state.season_series`)·공시 스트립 폭(`#disclosure`)·`settings` 게이팅 수정 후
`body.staged=true`(1920×1080 무대 모드, 에뮬레이트 뷰포트)로 재확인.

- [x] `body.staged=true`
- [x] 스테이지 수직 범위 0–1072px (1920×1080 무대 안에 들어옴)
- [x] `#disclosure` 하단 1054px ≤ 1080 — 5줄 모두 안 잘림 (스테이지 폭 상속, `.foot`의 1440px 제한 없음)
- [x] 무대 하단 행 카드 배치: `#seasonCard` 998–1434 × 742–1006, `#flyCard` 1448–1883 × 742–1006 (둘 다 무대 바닥 행 안)
- [x] 콘솔 오류 0, 한자(CJK) 0
- [x] 상단 스트립에 다음 펀딩 카운트다운 표시
- [x] 누적 실현손익 표시
- [x] ROI 표기에 유니코드 마이너스(U+2212) 사용 (하이픈 `-` 아님)

주의: 무대 재적용(re-fit)은 에뮬레이트 리사이즈 직후 바로 반영되지 않고 한 박자 뒤에 적용된다 —
측정 시 리사이즈 후 잠깐 대기해야 값이 안정된다.
